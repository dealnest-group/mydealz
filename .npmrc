# Project-level npm config — overrides any corporate registry settings in ~/.npmrc.
# This ensures every contributor installs from the public npm registry regardless
# of their personal/work npm configuration.

registry=https://registry.npmjs.org/
always-auth=false

# Pin pnpm settings used by CI so local dev matches the GitHub Actions runner.
auto-install-peers=true
strict-peer-dependencies=false
